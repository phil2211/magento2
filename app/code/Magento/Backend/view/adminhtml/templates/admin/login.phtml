<?php
/**
 * Copyright 2014 Adobe
 * All Rights Reserved.
 */

/**
 * @var \Magento\Framework\View\Element\AbstractBlock $block
 */
?>

<form method="post" action="" id="login-form" data-mage-init='{"form": {}, "validation": {}}' autocomplete="off">
    <fieldset class="admin__fieldset">
        <legend class="admin__legend">
            <span><?= $block->escapeHtml(__('Welcome, please sign in')) ?></span>
        </legend><br/>
        <input name="form_key" type="hidden" value="<?= $block->escapeHtmlAttr($block->getFormKey()) ?>" />
        <div class="admin__field _required field-username">
            <label for="username" class="admin__field-label">
                <span><?= $block->escapeHtml(__('Username')) ?></span>
            </label>
            <div class="admin__field-control">
                <input id="username"
                       class="admin__control-text"
                       type="text"
                       name="login[username]"
                       autofocus
                       value=""
                       data-validate="{required:true}"
                       placeholder="<?= $block->escapeHtmlAttr(__('user name')) ?>"
                       autocomplete="off"
                    />
            </div>
        </div>
        <div class="admin__field _required field-password">
            <label for="login" class="admin__field-label">
                <span><?= $block->escapeHtml(__('Password')) ?></span>
            </label>
            <div class="admin__field-control">
                <div class="admin__control-addon">
                    <input id="login"
                           class="admin__control-text"
                           type="password"
                           name="login[password]"
                           data-validate="{required:true}"
                           value=""
                           placeholder="<?= $block->escapeHtmlAttr(__('password')) ?>"
                           autocomplete="off"
                        />
                    <button type="button"
                            class="action-show-password admin__addon-suffix"
                            data-role="show-password"
                            aria-controls="login"
                            aria-pressed="false"
                            aria-label="<?= $block->escapeHtmlAttr(__('Show password')) ?>"
                            title="<?= $block->escapeHtmlAttr(__('Show password')) ?>"
                            data-show-label="<?= $block->escapeHtmlAttr(__('Show')) ?>"
                            data-hide-label="<?= $block->escapeHtmlAttr(__('Hide')) ?>"
                            data-show-password-label="<?= $block->escapeHtmlAttr(__('Show password')) ?>"
                            data-hide-password-label="<?= $block->escapeHtmlAttr(__('Hide password')) ?>"
                            data-mage-init='{"Magento_Backend/js/password-visibility-toggle": {"passwordSelector": "#login"}}'>
                        <span data-role="show-password-label"><?= $block->escapeHtml(__('Show')) ?></span>
                    </button>
                </div>
            </div>
        </div>
        <?= $block->getChildHtml('form.additional.info') ?>
        <div class="form-actions">
            <?= $block->getChildHtml('form.buttons') ?>
        </div>
    </fieldset>
</form>
